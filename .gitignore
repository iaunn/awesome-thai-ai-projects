# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo

# Raw / sensitive scrape data — contains actor IDs, profile URLs and live CDN
# session tokens (oh=/oe=/_nc_ohc). MUST stay untracked (REQUIREMENTS.md §7).
data/raw/
input.json

# Filter audit trail — regenerated by scripts/clean_data.py on every run.
# Not committed: the "name-tag" rejections are members' real full names, which
# is exactly the PII that REQUIREMENTS.md §3.1 requires us to strip. Anyone
# with data/raw/ can rebuild it locally to audit the filter.
data/filtered_out.json
